# Auto detect text files and perform LF normalization
* text=auto

# Force shell scripts to use LF line endings (required for Linux)
*.sh text eol=lf
render-build.sh text eol=lf

# Force batch files to use CRLF (Windows)
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
